[build]
builder = "NIXPACKS"
buildCommand = "npm ci --legacy-peer-deps && npx medusa build"

[deploy]
[deploy]
startCommand = "npx medusa db:migrate && node .medusa/server/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3